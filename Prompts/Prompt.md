CI = Individual Center Configuration: Add 3 new fields to the leavetype and the "Local settings -> absences and provisions" tab, "Absence type" tab: multiple SMALL INT UNSIGNED NULL. Set to 2 for individual center leave types (CA) and 1 for all others. If NULL, it is considered to be 1. iscyclerestricted TINYINT UNSIGNED NOT NULL (defaults to 0), set to 1 for CI for CA absence types. isfractionnable TINYINT UNSIGNED NOT NULL (defaults to 0), set to 1 for CI for CA absence types. Manage absences V2: If the isfractionnable parameter is set to 1, allow date-by-date submissions. A list of selected dates is displayed at the bottom, with the option to delete each date individually or all dates at once. Managing Absences V2: If the \`iscyclerestricted\` parameter is set to 1 for the selected absence type, once the agent has chosen a date, limit the selectable dates to the cycle to which the chosen date belongs. Managing Absences V2: If the \`multiple\` parameter is set to \`IS NOT NULL\` and \`!= 1\` for the selected absence type, display a red error message on the capture screen: "Error message for pairs. The message is: 'The number of days taken must be a multiple of {multiple}'." Do not accept client-side validation. On the server side, also verify that if \`multiple\` is set to \`IS NOT NULL\` and \`!= 1\`, the save operation will fail and display an error message to the user, the same as the one above on the client side.
