i want to create a TUI library that will be used to create TUI app using deno2

* the TUI must have a layout engine like flex box in CSS but more simple

* the TUI must handle overflow using scroll by default

* the TUI must handle text wrapping automatically.

* &#x20;make sure the TUI preserve select copy and paste using the default terminal shortcuts

* create a template color scheme that support FG BG color to show state like disabled focus .the layout can use borders too
  help me to design the architecture ho to manage this project

Create a prototype for game named hell diver a top-down shooter game the game is a bout a shuttle falling from the sky while shooting ground force the game is a 2 split screen the top showing the shuttle bottom is to show the ground force enemies create this using web technologies

jhg
