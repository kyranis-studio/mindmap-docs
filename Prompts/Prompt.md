@src/js/vue3/src/views/CapaView.vue  

For the CAPA page, find how the data is collected.The CAPA page has 2 modes normal mode that groups agents in bars and simulation mode.

The simulation mode duplicates the bars in normal mode using the agent count In simulation mode the user could modify the periods by agent. 

I need to add a save button to save the simulated mode back to the database. 

Take into consideration that if the user changes an agent period, it must be displayed as a separate bar in normal mode. 

Explain to me how the agent is grouped into the team what table are used to contruct

What data needs to be passed to the page to be able to save it back into the database? 

Write a plan to do that.
